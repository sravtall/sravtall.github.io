import { React } from "react";

const InfoPanel = () => {
  return (
    <div>
      <h3>Info Panel</h3>
      Info page (flexible, could make more specific)
    </div>
  );
};

export default InfoPanel;
